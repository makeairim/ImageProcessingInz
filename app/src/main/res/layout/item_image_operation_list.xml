<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="resource"
            type="pl.edu.agh.imageprocessing.data.local.dao.OperationWithChainAndResource" />
    </data>


        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_margin="5dp"
            android:background="@color/colorShadowEnd"
            android:gravity="bottom"
            android:clickable="true"
            android:focusable="true"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@android:color/black"
                android:layout_centerHorizontal="true"
                android:text="@{resource.operation.operationType}"
                android:layout_margin="5dp"
                tools:text="NAME"/>

                <ImageView
                    android:layout_below="@id/tv_name"
                    android:id="@+id/iv_photo"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:adjustViewBounds="true" />


            <!--android:text="@{@string/author(article.author)}"-->
        </RelativeLayout>
</layout>
